#!/bin/bash
# hypr-workspace-group — workspace group navigation
# Groups of DEVNS_GROUP_SIZE workspaces share a network namespace.
# Group 1 = ws 1-10, Group 2 = ws 11-20, etc.

. /usr/share/hyprflow/lib/devns-common.sh

STATE="${DEVNS_STATE_DIR}/active-group"

get_group() {
    cat "$STATE" 2>/dev/null || echo 1
}

save_slot() {
    local group="$1" slot="$2"
    echo "$slot" > "${DEVNS_STATE_DIR}/group-slot-${group}"
}

restore_slot() {
    cat "${DEVNS_STATE_DIR}/group-slot-${1}" 2>/dev/null || echo 1
}

# Get current slot (position within group) from active workspace
get_slot() {
    local ws_id group
    ws_id="$(hyprctl activeworkspace -j | jq -r '.id')"
    group="$(devns_ns_id "$ws_id")"
    echo "$(( ws_id - (group - 1) * DEVNS_GROUP_SIZE ))"
}

# Update waybar to show persistent workspaces for the active group + any group with windows
# Uses Hyprland workspace rules for instant display (no waybar reload).
# Falls back to hyprctl reload + SIGUSR2 only when removing stale workspaces.
WAYBAR_CONFIG="$HOME/.config/waybar/config.jsonc"
WAYBAR_PW_CACHE="${DEVNS_STATE_DIR}/waybar-pw"

update_waybar() {
    # Serialize concurrent calls (openwindow/closewindow fire in quick succession)
    exec 8>"${DEVNS_STATE_DIR}/waybar.lock"
    flock 8

    local active_group
    active_group="$(get_group)"

    # Get unique groups that have client windows
    local occupied_groups
    occupied_groups=$(hyprctl clients -j | jq -r --argjson gs "$DEVNS_GROUP_SIZE" \
        '[.[].workspace.id | select(. > 0) | (. - 1) / $gs + 1 | floor] | unique | .[]')

    # Merge active group + occupied groups, deduplicate
    local all_groups
    all_groups=$(printf '%s\n%s\n' "$active_group" "$occupied_groups" | sort -un | grep -v '^$')

    # Build desired workspace set and hyprctl batch command
    local ws_list="" batch="" pw="{" first=true
    local g i ws
    for g in $all_groups; do
        for i in $(seq 1 "$DEVNS_SLOTS_PER_GROUP"); do
            ws="$(devns_group_to_ws "$g" "$i")"
            ws_list+="$ws "
            batch+="keyword workspace $ws, persistent:true; "
            $first && first=false || pw+=","
            pw+="\"$ws\":[]"
        done
    done
    pw+="}"

    # Skip if unchanged
    local cached
    cached=$(cat "$WAYBAR_PW_CACHE" 2>/dev/null)
    if [ "$ws_list" = "$cached" ]; then
        exec 8>&-
        return
    fi

    # Check if any previously-shown workspaces are being removed
    local needs_removal=false
    if [ -n "$cached" ]; then
        for old_ws in $cached; do
            case " $ws_list" in
                *" $old_ws "*) ;;
                *) needs_removal=true; break ;;
            esac
        done
    fi

    echo "$ws_list" > "$WAYBAR_PW_CACHE"

    if [ "$needs_removal" = true ]; then
        # Removal path: clear stale Hyprland persistent workspaces, then reload waybar
        hyprctl reload
        hyprctl --batch "$batch" > /dev/null 2>&1
        local tmp
        tmp=$(mktemp)
        if jq --argjson pw "$pw" \
            '."hyprland/workspaces"."persistent-workspaces" = $pw' \
            "$WAYBAR_CONFIG" > "$tmp"; then
            mv "$tmp" "$WAYBAR_CONFIG"
        else
            rm -f "$tmp"
        fi
        pkill -SIGUSR2 waybar
    else
        # Addition-only path: instant via Hyprland IPC, no waybar reload
        hyprctl --batch "$batch" > /dev/null 2>&1
    fi

    exec 8>&-
}

case "$1" in
    init)
        update_waybar
        ;;
    switch)
        save_slot "$(get_group)" "$(get_slot)"
        echo "$2" > "$STATE"
        update_waybar
        hyprctl dispatch workspace "$(devns_group_to_ws "$2" "$(restore_slot "$2")")"
        ;;
    ws)
        hyprctl dispatch workspace "$(devns_group_to_ws "$(get_group)" "$2")"
        ;;
    move)
        hyprctl dispatch movetoworkspace "$(devns_group_to_ws "$(get_group)" "$2")"
        ;;
    movesilent)
        hyprctl dispatch movetoworkspacesilent "$(devns_group_to_ws "$(get_group)" "$2")"
        ;;
    movetogroup)
        slot=$(get_slot)
        hyprctl dispatch movetoworkspace "$(devns_group_to_ws "$2" "$slot")"
        ;;
    next)
        group=$(get_group)
        save_slot "$group" "$(get_slot)"
        new_group=$(( group + 1 ))
        echo "$new_group" > "$STATE"
        update_waybar
        hyprctl dispatch workspace "$(devns_group_to_ws "$new_group" "$(restore_slot "$new_group")")"
        ;;
    prev)
        group=$(get_group)
        save_slot "$group" "$(get_slot)"
        new_group=$(( group - 1 ))
        [ "$new_group" -lt 1 ] && new_group=1
        echo "$new_group" > "$STATE"
        update_waybar
        hyprctl dispatch workspace "$(devns_group_to_ws "$new_group" "$(restore_slot "$new_group")")"
        ;;
esac
